'use client';

import React, { ReactNode } from 'react';

/** VP tab chrome lives in the parent incentives/settings layout. */
const VpSettingsLayout = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};

export default VpSettingsLayout;
