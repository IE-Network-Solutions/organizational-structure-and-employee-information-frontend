export interface DepartmentsLevelsResponse {
  /**
   * Backend contract for `/departments/levels` is not typed in this repo yet.
   * Keep it flexible so consumers can adapt based on real API response.
   */
  [key: string]: any;
}
