// import { create } from 'zustand';

// type FormData = {
//   [key: string]: string | number | boolean | Date | null; // Adjust as needed for your field types
// };

// interface FormStore {
//   formData: FormData;
//   setFormData: (name: string, value: FormData[keyof FormData]) => void;
//   resetFormData: () => void;
// }

// export const useBiWeeklyStore = create<FormStore>((set) => ({

// }));
